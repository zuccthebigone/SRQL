export declare class User {
    username: string;
    passwordHash: string;
    fullname: string;
    id: string;
    constructor(username?: string, passwordHash?: string, fullname?: string, id?: string);
}
